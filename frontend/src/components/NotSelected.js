import React from 'react';
import { Colors } from '../constants/colors';

export default function NotSelected() {
  return (
    <div>
      <h6 style={{color:Colors.primary}}>Please select a program! </h6>
    </div>
  );
}
