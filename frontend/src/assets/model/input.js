class Input{
    constructor(id,input, output, constraints){
        this.id = id;
        this.input = input;
        this.output = output;
        this.constraints = constraints;
    }
}
export default Input;